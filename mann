#/bin/bash
# make annotation  makeann fontsize in_pic 'annotation' out_pic 
#if [[ "$1" == 'help' ]]; then
#r
#echo usage makeann fontsize vertposition  bgpic 'annotation' outpic
#exit
#fi

if [ -z $6 ];
then
echo "usage: $(basename $0)  color fontsize vertpos bgpic 'annotation' outpic
utfile"
echo
echo "Example: $(basename $0) yellow 30  340 bg.png 'text' out.png"
exit
fi

magick -fill $1 $4 -gravity North -pointsize $2 -font Noto-Serif -annotate +0+$3 "$5"  $6	
