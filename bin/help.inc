 vert2="\nScript for normalizing video $VER  \n\nUsage:\n${BLUE}${NAME} -p standartpic -i inputvideo -o outvideo${NC}\n Example:\n${BLUE}${NAME} -p pv720.png -i input.mp4 -o output.mp4${NC} \n${CYAN}Example corrects video 720x1280 to SAR 1:1, DAR 9:16 after crop and resize${NC}\n" 
 vpart2="\nScript for partial download from youtube $VER\n\nUsage \n ${BLUE} $(basename $0) url start_time duration outfile.mp4 ${NC} \nExample\n ${BLUE} $(basename $0)  https://youtu.be/P6xmSZCIZpI?si=dOuQgcDWPZAbl7m 1:00 10 out.mp4${NC} \nor Example\n ${BLUE} $(basename $0)  https://youtu.be/P6xmSZCIZpI?si=dOuQgcDWPZAbl7m 1:00 full out.mp4${NC} "
 annotate="Make annotation\nUsage \n ${BLUE} $(basename $0) fontcolor bgpic 'annotation' outpic ${NC}\nExample\n ${BLUE} $(basename $0)  yellow bg.png 'Lorem ipsum' out.png${NC} "
 getaudio="\nDownload only audio from youtube $VER\nUsage\n${BLUE} $(basename $0) start_time end_time  youtube_url outfile.mp3 or set start_time 0, end_time full to get full audio stream.${NC} \n${GREEN} Example:\n${YELLOW}$(basename $0) 1:25 10 some_yt_url out.mp3\n${YELLOW}$(basename $0) 0 full some_yt_url out.mp3"${NC}
 crop="\n Script for cropping video $VER\n\n Usage:\n${BLUE}$(basename $0) inputfile starttime endtime crop_h_size  crop_v_size crop_h_offset crop_v_offset outputfile\nor set end_time full for whole file.\n ${NC}\nExample\n${YELLOW}crop input.mp4 0 7 600 800 100 0 out.mp4\ncrop input.mp4 0 full 600 800 100 0 out.mp4${NC}"
 vpart2ng="\n${CYAN}Script for part download \nfrom youtube ver. $VER \n\n${BLUE}-u url \n-s start_time \n-d duration \n-o out_file \n-b bitrate video \n-c copy(-b -g will be ignored) \n-g scale (e.g. 1280x720)\n\n${BROWN}$(basename $0) -u some_url -s 1:00 -d 20 -b 1M -g 1280x720 -o output.mp4\n  ${NC}"